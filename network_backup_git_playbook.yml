---
- name: network device backup to git
#  hosts: routers
#  hosts: rtr3
  hosts: rtr3
  gather_facts: false
  vars:
    backup_dir: "{{ playbook_dir }}/net_backups"
    backup_file: "{{ backup_dir }}/{{ inventory_hostname }}"
    backup_repo: git@github.com:beasleymd/Backups.git
    git_name: Git Backup
    git_email: beasleymd@yahoo.com
  
  tasks:

  - import_role:
      name: network_backup_git
